

def get_inventory_status(sku: str, region: str) -> str:
    # Mock implementation (replace with API later)
    return (
        f"Inventory for {sku} in {region}: "
        "120 units available, reorder threshold is 100."
    )
